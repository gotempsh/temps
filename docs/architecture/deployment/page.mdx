export const metadata = {
  title: "Deployment Pipeline",
  description:
    "Learn how Temps builds and deploys your applications from Git. Understand the build pipeline, Docker image creation, and zero-downtime rollout process.",
  alternates: {
    canonical: "/docs/deployment",
  },
};

export const sections = [
  { title: 'What happens when you deploy', id: 'what-happens-when-you-deploy' },
  { title: 'What you can control', id: 'what-you-can-control' },
  { title: 'Deployment states', id: 'deployment-states' },
  { title: 'Rollback', id: 'rollback' },
  { title: 'Build failures', id: 'build-failures' },
  { title: 'Monitoring deployments', id: 'monitoring-deployments' },
]

# Deployment Pipeline

When you deploy an application, Temps automatically handles the entire process from Git to production — cloning the repository, building a container image, running health checks, and routing traffic. {{ className: 'lead' }}

<Mermaid
  chart={`
sequenceDiagram
    participant User
    participant Temps
    participant Git
    participant Builder
    participant Container
    participant SSL

    User->>Temps: Deploy from Git
    Temps->>Git: Clone Repository
    Git-->>Temps: Source Code
    Temps->>Builder: Detect Language & Build
    Builder->>Builder: Build Container Image
    Builder-->>Temps: Image Ready
    Temps->>Container: Start Container
    Container->>Container: Health Check
    Container-->>Temps: Running
    Temps->>SSL: Request Certificate
    SSL-->>Temps: Certificate Issued
    Temps-->>User: App Live
`}
/>

---

## What happens when you deploy {{ anchor: true, id: 'what-happens-when-you-deploy' }}

### Step 1: Connect your repository

- Link your GitHub, GitLab, or other Git repository
- Temps clones your code
- Automatically detects your language and framework

### Step 2: Build your application

**If you have a Dockerfile:**

- Temps uses your Dockerfile to build the container
- Supports all Docker features and build arguments
- Optimized builds with layer caching

**If you do not have a Dockerfile:**

- Temps automatically detects your language
- Generates an appropriate Dockerfile for you
- Works for Node.js, Python, Rust, Go, Java, Ruby, PHP, and more
- Zero configuration needed

### Step 3: Configure your environment

- Set environment variables in the dashboard
- Use secrets for sensitive data (encrypted storage)
- Different variables for dev, staging, and production
- Automatic database credentials if using managed databases

### Step 4: Deploy the container

- Container starts with your environment variables
- Health checks ensure your app is running
- Automatic retries if startup fails
- Resource limits (CPU, memory) as configured

### Step 5: Set up domain and SSL

- Assign a custom domain or use auto-generated subdomain
- Automatic SSL certificate from Let's Encrypt
- Certificates renew automatically
- Your app is live and secure

---

## What you can control {{ anchor: true, id: 'what-you-can-control' }}

### Build configuration

- **Use your Dockerfile** — Full control with custom Dockerfiles
- **Auto-detection** — Let Temps figure it out automatically
- **Build arguments** — Pass custom build-time variables
- **Build logs** — See exactly what is happening during the build

### Environment variables

- **Project-level** — Variables shared across all environments
- **Environment-level** — Specific to development, staging, or production
- **Secrets** — Encrypted storage for passwords and API keys
- **Override** — Override variables for specific deployments

### Resource limits

- **Memory** — Set how much RAM your application can use
- **CPU** — Limit CPU usage
- **Disk** — Storage limits
- **Timeouts** — Request timeout settings

### Deployment settings

- **Port configuration** — Which port your application listens on
- **Health check path** — Custom health check endpoint
- **Restart policy** — Automatic restarts on failure
- **Rollback** — Instantly revert to a previous deployment

---

## Deployment states {{ anchor: true, id: 'deployment-states' }}

Your deployment goes through these states:

<Mermaid
  chart={`
stateDiagram-v2
    [*] --> PENDING: Deploy Requested
    PENDING --> BUILDING: Start Build
    BUILDING --> BUILT: Build Complete
    BUILDING --> FAILED: Build Error
    BUILT --> DEPLOYING: Start Container
    DEPLOYING --> RUNNING: Container Started
    DEPLOYING --> FAILED: Start Error
    RUNNING --> ACTIVE: Health Check Passed
    RUNNING --> FAILED: Health Check Failed
    ACTIVE --> [*]: Deployment Live
    FAILED --> [*]: Deployment Failed
`}
/>

**PENDING** — Deployment queued
**BUILDING** — Building your container
**BUILT** — Container image created
**DEPLOYING** — Starting the container
**RUNNING** — Container is running
**ACTIVE** — Receiving live traffic

If something goes wrong, the state becomes **FAILED** and the deployment detail page shows error details.

---

## Rollback {{ anchor: true, id: 'rollback' }}

If a deployment has issues:

1. **Previous deployment** is still running
2. **Switch routing** — Point the domain back to the previous version
3. **Zero downtime** — Users see the working version immediately
4. **Debug** — Investigate the failed deployment
5. **Delete** — Remove the failed deployment when ready

---

## Build failures {{ anchor: true, id: 'build-failures' }}

### Common issues and solutions

**Language detection failed**

- **Solution:** Provide a Dockerfile
- **Why:** Temps could not detect your project type

**Dependency installation failed**

- **Solution:** Check your `package.json`, `requirements.txt`, `go.mod`, etc.
- **Why:** Missing or incorrect dependency files

**Port binding failed**

- **Solution:** Set the `PORT` environment variable
- **Why:** The application is trying to use a port that is already in use

**Memory exceeded**

- **Solution:** Increase the memory limit in deployment settings
- **Why:** The application needs more memory than allocated

---

## Monitoring deployments {{ anchor: true, id: 'monitoring-deployments' }}

### Real-time information

- **Build logs** — Watch build progress in real-time
- **Deployment logs** — See application startup logs
- **Health status** — Know when your application is healthy
- **Resource usage** — Monitor CPU, memory, and disk usage

### After deployment

- **Analytics** — Track request volume and performance
- **Error tracking** — See errors as they happen
- **Uptime** — Monitor availability
- **Performance** — Track response times

---

## Next steps

- **[Overview](/docs/overview)** — System capabilities
- **[Data Flow](/docs/data-flow)** — Analytics and monitoring
- **[Security Architecture](/docs/security)** — Security features
