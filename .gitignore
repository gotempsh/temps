# Local
.DS_Store
*.local
*.log*

# Dist
node_modules
dist/

# IDE
.vscode/*
!.vscode/extensions.json
.idea
.vscode
.claude
.*-mcp
.classpath
.project
.settings/
target
target2
/ai

.cargo/config.toml
*.crt
*.key

target_old

*.mp4
*.rec

.env
.env.local

# Content / blog drafts
content/

__pycache__

.claude
.agents
skills-lock.json

# MaxMind GeoLite2 database — has restrictive license, do not commit.
# The dev cluster reads this from /workspace/GeoLite2-City.mmdb (i.e. the
# repo root) via the bind mount. Copy from ~/.temps/ once.
GeoLite2-*.mmdb

.next

# Stray build artifacts dropped at repo root from local cross-builds
# (e.g. `cargo build --target x86_64-unknown-linux-gnu` copied here).
# These are 250+ MB binaries that should never be committed.
/temps-new
/temps-old

# Private telemetry dashboard — visualizes the anonymous product-telemetry
# data. The ingest API + schema (telemetry-api/) stay PUBLIC for transparency,
# but the dashboard contains internal business judgment (which metrics we watch,
# what "good" looks like) and must NOT be open-sourced. See telemetry-api/ for
# the public, auditable side.
/telemetry-dashboard/
