// SPDX-FileCopyrightText: 2024-2026 Temps Contributors
// SPDX-License-Identifier: MIT OR Apache-2.0

use sea_orm_migration::prelude::*;

#[derive(DeriveMigrationName)]
pub struct Migration;

#[async_trait::async_trait]
impl MigrationTrait for Migration {
    async fn up(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager
            .get_connection()
            .execute_unprepared(
                "ALTER TABLE visitor_activity_reports
             ADD COLUMN run_history JSONB NOT NULL DEFAULT '[]'::jsonb,
             ADD COLUMN visitor_checkpoints JSONB NOT NULL DEFAULT '{}'::jsonb",
            )
            .await?;
        Ok(())
    }

    async fn down(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager
            .get_connection()
            .execute_unprepared(
                "ALTER TABLE visitor_activity_reports
             DROP COLUMN visitor_checkpoints, DROP COLUMN run_history",
            )
            .await?;
        Ok(())
    }
}
