//! Backwards-compatible re-exports for preset configuration schemas.
//!
//! The canonical API schema lives in `temps-presets` alongside the catalog and
//! storage resolution contract so the two cannot drift independently.

pub use temps_presets::preset_config_schema::*;
