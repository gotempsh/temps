{
	"name": "@temps-sdk/svelte-analytics",
	"version": "0.0.1",
	"description": "Temps analytics for Svelte — stores, actions, and helpers for events, Web Vitals, engagement, and session replay.",
	"private": false,
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build:js": "tsc -p tsconfig.json",
		"build:types": "tsc -p tsconfig.types.json",
		"build": "bun run build:js && bun run build:types",
		"clean": "rm -rf dist",
		"prepublishOnly": "bun run clean && bun run build"
	},
	"publishConfig": {
		"access": "public"
	},
	"peerDependencies": {
		"svelte": "^4.2.20 || >=5.53.5"
	},
	"dependencies": {
		"@temps-sdk/analytics-core": "workspace:*"
	},
	"devDependencies": {
		"svelte": "^5.53.5",
		"typescript": "^5.5.4"
	},
	"sideEffects": false
}
