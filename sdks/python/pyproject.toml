[project]
name = "temps-plugin-sdk"
version = "0.1.0"
description = "Python SDK for building Temps external plugins"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.5",
]

[tool.uv]
dev-dependencies = [
    "pyinstaller>=6.0",
    "pyright>=1.1",
]
